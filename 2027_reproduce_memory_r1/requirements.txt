# Direct dependencies for Memory-R1 reproduction — unpinned so the resolver picks whatever
# is available on your PyPI mirror. For a fully-pinned reproducible install, see
# requirements.lock.txt (generated via `uv pip compile pyproject.toml`).
#
# Usage:
#   pip install -r requirements.txt

# Core deep learning
torch
transformers
accelerate
peft
bitsandbytes; sys_platform != "darwin"

# RL fine-tuning (PPO / GRPO). trl>=0.12 has GRPOTrainer.
trl
deepspeed; sys_platform != "darwin"

# Retrieval / embeddings
sentence-transformers
faiss-cpu

# Data / datasets
datasets
huggingface-hub

# LLM APIs (fact extraction + LLM-as-a-Judge)
openai
tenacity

# Metrics
sacrebleu
nltk
rouge-score
scikit-learn

# Utilities
pydantic
pyyaml
tqdm
numpy
pandas
rich
typer
loguru

# Experiment tracking
wandb

# Networking / robust downloads
requests
gdown

# Testing
pytest
